# workspace/ — rascunho, não entrega

Scripts de uma vez, consultas exploratórias, saídas de auditoria. Nada aqui é
lido pela aplicação nem por outra sessão.

**Ignorado pelo git** (ver `.gitignore`): o que estiver aqui não vai para o
repositório. Se algo merece sobreviver, mova para `docs/` ou vire comentário
no código, que é onde este projeto guarda o porquê das coisas.
